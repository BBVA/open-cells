{
  "name": "<%= name %>",
  "version": "0.1.0",
  "private": true,
  "description": "An application built using Open Cells and Lit",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.15.0",
    "npm": ">=7.0.0"
  },
  "type": "module",
  "scripts": {
    "start": "npm run serve",
    "serve": "vite",
    "build": "tsc --outDir dist && vite build --outDir dist",
    "preview": "npm run build && vite preview"
  },
  "dependencies": {
    "@material/web": "^1.4.0",
    "@open-cells/core": "^1.0.0-beta.1",
    "@open-cells/element-controller": "^1.0.0-beta.2",
    "@open-cells/page-controller": "^1.0.0-beta.2",
    "@open-cells/page-transitions": "^1.0.0-beta.1",
    "lit": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.7"
  }
}
